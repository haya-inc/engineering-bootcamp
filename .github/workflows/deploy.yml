name: Deploy site to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "site/**"
      - "textbook/book/**"
      - "textbook/glossary.md"
      # 台本は各動画ページに埋め込まれる正本。変更時もサイトを再生成・再デプロイする。
      - "video-scripts/**"
      - "README.md"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# 同時デプロイは1つに絞る (進行中はキャンセルしない)
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: site
    steps:
      - uses: actions/checkout@v4

      # bun = package manager / vp(品質ゲート) の実行系。mise.toml と同一版に固定。
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      # astro / split-book は node ランタイムで動く。patch まで固定し再現性を担保。
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "26.3.1"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # astro:content の型を生成してから型チェックする (本文を先に分割)
      - name: Generate content & Astro types
        run: |
          node scripts/split-book.mjs
          bunx astro sync

      # 品質ゲート (Vite+ = alpha なので check/test のみ。本番ビルドは下の脱出ハッチで vp 非依存)
      - name: Format & lint (oxfmt + oxlint)
        run: bunx vp check
      - name: Type check (astro check)
        run: bunx astro check
      - name: Test (vitest)
        run: bunx vp test run

      # 本番ビルドは vp を経由しない素のスクリプト (alpha ツールの単一点障害を避ける脱出ハッチ)
      - name: Build (split → astro build → pagefind)
        run: bun run build

      # 生成が無動作だと空サイトになるため、出力 dist のページ枚数を別観点から検査する。
      # split-book.mjs の章番号検査 (assertChapters) とは相補的: あちらは「正本の章が 1..24 揃うか」、
      # ここは「実際に静的 HTML が出力されたか」を見る。videos は詳細ページ (mindepth 2) だけ数える
      # (一覧 index を含めない = 閾値24が「動画詳細24本」と一致して意図が明確になる)。
      - name: Assert pages generated
        run: |
          tb=$(find dist/textbook -name index.html | wc -l)
          gl=$(find dist/glossary -name index.html | wc -l)
          vd=$(find dist/videos -mindepth 2 -name index.html | wc -l)
          echo "textbook=$tb glossary=$gl videos(detail)=$vd"
          test "$tb" -ge 28 || { echo "::error::textbook pages too few ($tb)"; exit 1; }
          test "$gl" -ge 24 || { echo "::error::glossary pages too few ($gl)"; exit 1; }
          test "$vd" -ge 24 || { echo "::error::video detail pages too few ($vd)"; exit 1; }

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
